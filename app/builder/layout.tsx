import React from "react";

export const metadata = {
  title: "Builder – Prestige Build",
  description: "Créez une application en décrivant ce que vous voulez en langage naturel.",
};

export default function BuilderLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
