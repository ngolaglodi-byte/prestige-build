import React from "react";

export const metadata = {
  title: "Éditeur visuel – Prestige Build",
  description: "Construisez votre application visuellement par glisser-déposer.",
};

export default function EditorLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
